<nav class="navbar navbar-expand-md bg-light border-bottom sticky-top" id="mainNavbar">
  <div class="container-fluid">
    <!-- Brand Icon -->
    <a class="navbar-brand" href="/">
      <i class="fa-brands fa-opencart"></i>
    </a>

    <!-- Navbar Toggler -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Navbar Content -->
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link" href="/" style="color:black">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/listings" style="color:black">All List</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/listings/new" style="color:black">Join in</a>
        </li>
      </ul>

      <!-- Right Side - User Auth Links -->
      <ul class="navbar-nav ms-auto">
        <% if (!currUser) { %>
          <li class="nav-item">
            <a class="nav-link" href="/signup" style="color:black">SignUp</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/login" style="color:black">Login</a>
          </li>
        <% } else { %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              Welcome, <%= currUser.username %>  
            </a>
            <ul class="dropdown-menu dropdown-menu-end custom-dropdown">
              <!-- <li><a class="dropdown-item" href="/profile">Profile</a></li> -->
              <li><a class="dropdown-item logout-btn" href="/logout">Logout</a></li>
            </ul>
          </li>
        <% } %>
        
        <!-- Dark Mode Toggle -->
        <!-- <li class="nav-item">
          <button class="btn btn-outline-dark ms-3" id="themeToggle">Dark Mode</button>
        </li> -->
      </ul>
    </div>
  </div>
</nav>

<!-- JavaScript for Dark Mode Toggle -->
<script>
  const themeToggle = document.getElementById("themeToggle");
  const navbar = document.getElementById("mainNavbar");

  themeToggle.addEventListener("click", () => {
    document.body.classList.toggle("dark-mode");
    navbar.classList.toggle("bg-dark");
    navbar.classList.toggle("navbar-dark");
    
    if (document.body.classList.contains("dark-mode")) {
      themeToggle.textContent = "Light Mode";
    } else {
      themeToggle.textContent = "Dark Mode";
    }
  });
</script>

<!-- CSS for Dropdown Styling & Dark Mode -->
<style>
  /* Navbar Dropdown Styling */
  .custom-dropdown {
    border-radius: 10px;
    border: 1px solid #ddd;
    box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
    transition: all 0.3s ease-in-out;
  }

  .custom-dropdown .dropdown-item {
    padding: 10px 15px;
    transition: background-color 0.3s;
  }

  .custom-dropdown .dropdown-item:hover {
    background-color: #f8f9fa;
  }

  .logout-btn {
    color: red !important;
    font-weight: bold;
  }

  /* Dark Mode */
  .dark-mode {
    background-color: #121212;
    color: white;
  }

  .dark-mode .nav-link, .dark-mode .dropdown-item {
    color: white !important;
  }

  .dark-mode .dropdown-menu {
    background-color: #333;
    border: 1px solid #444;
  }

  .dark-mode .dropdown-item:hover {
    background-color: #555;
  }
</style>
