const mongoose=require("mongoose");
const Schema=new mongoose.Schema;

const listingSchema=new Schema({
    title:String,
    description:Strin
    image:String,
    Price:Number,
    location:String,
    CountryString,
})